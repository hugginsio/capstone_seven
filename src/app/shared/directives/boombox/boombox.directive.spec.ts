import { BoomboxDirective } from './boombox.directive';

describe('BoomboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BoomboxDirective();
    expect(directive).toBeTruthy();
  });
});
